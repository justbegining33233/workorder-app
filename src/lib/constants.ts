/**
 * FixTray platform constants
 */

/** Fixed FixTray service fee charged on every work order (in USD) */
export const FIXTRAY_SERVICE_FEE = 5.00;

/** FixTray service fee in cents (for Stripe PaymentIntent amounts) */
export const FIXTRAY_SERVICE_FEE_CENTS = 500;
